Files changed and why
- `.hydra-result-hydra-2ece8088044abe89.md`: recorded the review findings for `857231445e6afe1f25d5c6623f7c3729a051e183` as requested.

Issues found
- High: `src/components/ComposerBar.tsx:266` reintroduces the stale-focus race that `e00c2b6` explicitly fixed. `handleSubmit` awaits `window.termcanvas.composer.submit(request)`, and bracketed-paste submits can stay in flight across multiple `delayMs` calls in `electron/composer-submit.ts`. During that window the user can switch focus to another terminal. The deleted code re-read `useProjectStore.getState().projects` and skipped textarea refocus when the newly focused terminal was `inputMode === "type"`. With the new unconditional `requestAnimationFrame(() => textareaRef.current?.focus())`, finishing an older submit now steals DOM focus back to the composer even if the user has already switched to a shell/tmux/lazygit terminal whose xterm should own focus. That is a real regression in terminal switching behavior.
- Medium: the change claims to "always refocus textarea after submit", but focus restoration still only happens on the success path. `src/components/ComposerBar.tsx:255-259` returns early for `!result.ok`, and `src/components/ComposerBar.tsx:267-271` handles thrown errors, but neither path schedules any focus restore before `finally` clears `isSubmitting`. Because the textarea was disabled during submission, failed submits still leave it blurred, so retry-after-error focus behavior remains broken.

Whether tests pass
- Passed: `node --experimental-strip-types --test tests/composer-targeting.test.ts tests/composer-submit.test.ts`
- Note: there is no automated coverage here for the `ComposerBar` focus race or failure-path focus restoration.

Unresolved problems
- The review findings above were not fixed in this worktree; only the review artifact was added.
