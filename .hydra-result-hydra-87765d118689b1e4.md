Files changed and why

- `src/stores/projectStore.ts`: fixed `resolveWorktreeOverlaps` so each worktree is checked against all earlier worktrees in the vertical sweep, which reflows later same-lane worktrees even when an off-lane worktree sits between them in sort order.
- `tests/project-store-sync-worktrees.test.ts`: added regressions for cascaded overlap resolution during `syncWorktrees` and for `addTerminal` growing a worktree and reflowing every later overlapping worktree.

Issues found

- `resolveWorktreeOverlaps` only compared each worktree with the immediately previous item in the initial `y` sort. That missed overlaps when a moved worktree still had another same-lane worktree below it, separated by a non-overlapping sibling on a different `x` lane.

Whether tests pass

- Passed: `node --experimental-strip-types --test tests/project-store-sync-worktrees.test.ts tests/project-store-focus.test.ts`

Any unresolved problems

- None found in the scoped task.
