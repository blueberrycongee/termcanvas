# Hydra Result

## Files changed

1. **src/hooks/shortcutTarget.ts** — Exported `hasPrimaryModifier` with an optional `platform` parameter (defaults to `window.termcanvas?.app.platform ?? "darwin"`). This is the canonical definition now.

2. **src/stores/shortcutStore.ts** — Removed the duplicate `hasPrimaryModifier` definition and imported the shared version from `../hooks/shortcutTarget`. All call sites already passed `platform` explicitly, so they work unchanged.

3. **src/components/ComposerBar.tsx** — Changed `draft.length === 0` to `draft.trim().length === 0` in `getPassthroughSequence` (line 115), so whitespace-only draft is treated as empty when deciding whether to forward arrow keys to the terminal.

## Tests

- `npx vitest run` fails on **all** test files with a pre-existing `ReferenceError: require is not defined in ES module scope` — this is a vitest configuration issue unrelated to these changes and also reproduces on the unmodified code.
- `npx tsc --noEmit` passes cleanly — no type errors introduced.

## Unresolved problems

- The vitest runner is broken project-wide (pre-existing). Tests use `node:test` imports which conflict with vitest's ESM transform.
